[project]
name = "eva"
version = "0.1.0"
description = "Eva acts as a smart proxy agent between clients (or AI agents) and the core ArchiveNet services."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
